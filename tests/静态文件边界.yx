引「../src/言序Web.yx」为 言枢；

引「包:yanxu-http」为 HTTP；

引「标准:测试」为 测试；

引「标准:字节」为 字节；

引「标准:文件」为 文件；

令 静态根：文 为 「tests/fixtures」；

若 文件.存在（「yanxu-web/tests/fixtures/site.css」） 则
    置 静态根 为 「yanxu-web/tests/fixtures」；
终

法 请求为（应用，路径：文，首部：典） 则
    令 原文：文 为 （（「GET 」 加 路径） 加 「 HTTP/1.1\nHost: testserver」）；
    逐 名称：文 于 键列（首部） 则
        置 原文 为 （（（原文 加 「\n」） 加 名称） 加 （「: 」 加 首部【名称】））；
    终
    归 应用.处理（HTTP.解析请求头（原文））；
终

定 配置项 为 言枢.静态配置（）；

定 应用 为 言枢.创建应用（言枢.配置（））；

应用.挂静态为（「/assets」，静态根，配置项）；

定 完整 为 请求为（应用，「/assets/site.css」，{}）；

测试.相等（完整.状态码，200）；

测试.相等（完整.首部值（「content-type」），「text/css; charset=utf-8」）；

测试.相等（完整.首部值（「cache-control」），「public, max-age=0, must-revalidate」）；

测试.相等（完整.首部值（「accept-ranges」），「bytes」）；

测试.相等（完整.首部值（「x-content-type-options」），「nosniff」）；

测试.相等（字节.长度（完整.正文），22）；

令 ETag：文 为 「」；

定 ETag值：文? 为 完整.首部值（「etag」）；

若 （ETag值 是 文） 则
    置 ETag 为 ETag值；
终

测试.断言（（长度（ETag） 大于 0），「静态文件须提供 ETag」）；

令 修改日期：文 为 「」；

定 修改日期值：文? 为 完整.首部值（「last-modified」）；

若 （修改日期值 是 文） 则
    置 修改日期 为 修改日期值；
终

测试.断言（（长度（修改日期） 大于 0），「静态文件须提供 Last-Modified」）；

定 标签未修改 为 请求为（应用，「/assets/site.css」，{「If-None-Match」：ETag}）；

测试.相等（标签未修改.状态码，304）；

测试.相等（字节.长度（标签未修改.正文），0）；

测试.相等（标签未修改.首部值（「etag」），ETag）；

测试.相等（请求为（应用，「/assets/site.css」，{「If-None-Match」：「*」}）.状态码，304）；

测试.相等（请求为（应用，「/assets/site.css」，{「If-Modified-Since」：修改日期}）.状态码，304）；

测试.相等（请求为（应用，「/assets/site.css」，{「If-None-Match」：「W/"other"」，「If-Modified-Since」：修改日期}）.状态码，200）；

定 首范围 为 请求为（应用，「/assets/site.css」，{「Range」：「bytes=0-3」}）；

测试.相等（首范围.状态码，206）；

测试.相等（字节.转文字（首范围.正文），「body」）；

测试.相等（首范围.首部值（「content-range」），「bytes 0-3/22」）；

定 开放范围 为 请求为（应用，「/assets/site.css」，{「Range」：「bytes=5-」}）；

测试.相等（开放范围.状态码，206）；

测试.相等（字节.长度（开放范围.正文），17）；

定 后缀范围 为 请求为（应用，「/assets/site.css」，{「Range」：「bytes=-2」}）；

测试.相等（后缀范围.状态码，206）；

测试.相等（字节.转文字（后缀范围.正文），「}\n」）；

测试.相等（请求为（应用，「/assets/site.css」，{「Range」：「bytes=20-99」}）.首部值（「content-range」），「bytes 20-21/22」）；

定 不满足 为 请求为（应用，「/assets/site.css」，{「Range」：「bytes=22-30」}）；

测试.相等（不满足.状态码，416）；

测试.相等（不满足.首部值（「content-range」），「bytes */22」）；

测试.相等（请求为（应用，「/assets/site.css」，{「Range」：「bytes=0-1,4-5」}）.状态码，416）；

测试.相等（请求为（应用，「/assets/site.css」，{「Range」：「items=0-1」}）.状态码，200）；

测试.相等（请求为（应用，「/assets/site.css」，{「Range」：「bytes=0-3」，「If-Range」：ETag}）.状态码，200）；

测试.相等（请求为（应用，「/assets/site.css」，{「Range」：「bytes=0-3」，「If-Range」：修改日期}）.状态码，206）；

测试.相等（请求为（应用，「/assets/missing.css」，{}）.状态码，404）；

测试.相等（言枢.静态文件响应（静态根，「templates」）.状态码，404）；

令 已拒绝隐藏：理 为 假；

试 则
    言枢.静态文件响应（静态根，「.secret.txt」）；
救 所误 则
    置 已拒绝隐藏 为 真；
终

测试.断言（已拒绝隐藏，「默认静态配置须拒绝隐藏文件」）；

定 隐藏配置 为 言枢.静态配置（）.设允许隐藏文件（真）；

测试.相等（隐藏配置.允许隐藏文件，真）；

令 已拒绝绝对路径：理 为 假；

试 则
    言枢.静态文件响应（静态根，「/etc/passwd」）；
救 所误 则
    置 已拒绝绝对路径 为 真；
终

测试.断言（已拒绝绝对路径，「静态文件须拒绝绝对路径」）；

令 已拒绝反斜线：理 为 假；

试 则
    言枢.静态文件响应（静态根，「templates\\base.yb.html」）；
救 所误 则
    置 已拒绝反斜线 为 真；
终

测试.断言（已拒绝反斜线，「静态文件须拒绝反斜线路径」）；

定 无条件配置 为 言枢.静态配置（）.设条件请求（假）.设范围请求（假）.设缓存控制（「public, max-age=60」）；

定 无条件应用 为 言枢.创建应用（言枢.配置（））；

无条件应用.挂静态为（「/plain」，静态根，无条件配置）；

定 无条件响应 为 请求为（无条件应用，「/plain/site.css」，{「If-None-Match」：ETag，「Range」：「bytes=0-3」}）；

测试.相等（无条件响应.状态码，200）；

测试.相等（无条件响应.首部值（「accept-ranges」），空）；

测试.相等（无条件响应.首部值（「cache-control」），「public, max-age=60」）；

定 小预算配置 为 言枢.静态配置（）.设最大文件字节（4）；

定 小预算应用 为 言枢.创建应用（言枢.配置（））；

小预算应用.挂静态为（「/small」，静态根，小预算配置）；

测试.相等（请求为（小预算应用，「/small/site.css」，{}）.状态码，404）；

令 已拒绝小预算：理 为 假；

试 则
    言枢.静态文件响应为（静态根，「site.css」，小预算配置）；
救 所误 则
    置 已拒绝小预算 为 真；
终

测试.断言（已拒绝小预算，「静态文件读取前须执行大小预算」）；

令 已拒绝缺失根：理 为 假；

试 则
    言枢.静态处理器（「tests/not-found-static-root」）；
救 所误 则
    置 已拒绝缺失根 为 真；
终

测试.断言（已拒绝缺失根，「静态处理器须在建立时验证文件根」）；

定 被篡改配置 为 言枢.静态配置（）；

置 被篡改配置.最大文件字节 为 0；

令 已二次验证配置：理 为 假；

试 则
    言枢.静态文件响应为（静态根，「site.css」，被篡改配置）；
救 所误 则
    置 已二次验证配置 为 真；
终

测试.断言（已二次验证配置，「静态响应入口须二次验证公开配置字段」）；
