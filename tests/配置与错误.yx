引「../src/言序Web.yx」为 言枢；

引「标准:测试」为 测试；

法 首页（上下文） 则
    归 言枢.文字响应（「首页」）；
终

法 错误值（上下文） 则
    归 「不是响应」；
终

法 错误处理值（上下文，所误） 则
    归 「仍不是响应」；
终

法 回声（上下文） 则
    归 言枢.文字响应（上下文.正文文字（））；
终

法 长响应（上下文） 则
    归 言枢.文字响应（「响应过长」）；
终

法 空中间件（上下文，下一步） 则
    归 下一步（）；
终

法 取错误代码（动作：法）：文 则
    试 则
        动作（）；
    救 所误 则
        归 言枢.错误详情（所误）【「代码」】；
    终
    归 「」；
终

定 配置项 为 言枢.配置（）.设应用名（「配置规格」）.设调试（真）.设模板根（「tests/fixtures/templates」）.设静态根（「tests/fixtures」）.设内容安全策略（「default-src 'none'」）；

测试.相等（配置项.应用名，「配置规格」）；

测试.相等（配置项.调试，真）；

测试.相等（配置项.模板根，「tests/fixtures/templates」）；

定 应用 为 言枢.创建应用（配置项）；

应用.取（「/」，首页）；

定 响应 为 言枢.测试客户端（应用）.取（「/」）；

测试.相等（响应.首部值（「x-content-type-options」），「nosniff」）；

测试.相等（响应.首部值（「content-security-policy」），「default-src 'none'」）；

定 无首部配置 为 言枢.配置（）.设自动安全首部（假）；

定 无首部应用 为 言枢.创建应用（无首部配置）；

无首部应用.取（「/」，首页）；

测试.相等（言枢.测试客户端（无首部应用）.取（「/」）.首部值（「x-content-type-options」），空）；

定 错误应用 为 言枢.应用（）；

错误应用.取（「/bad」，错误值）；

测试.相等（言枢.测试客户端（错误应用）.取（「/bad」）.状态码，500）；

错误应用.设错误处理器（错误处理值）；

令 已拒绝错误处理值：理 为 假；

试 则
    言枢.测试客户端（错误应用）.取（「/bad」）；
救 所误 则
    置 已拒绝错误处理值 为 真；
终

测试.断言（已拒绝错误处理值，「错误处理器必须返回 HTTP 响应」）；

令 已拒绝空应用名：理 为 假；

试 则
    言枢.配置（）.设应用名（「」）；
救 所误 则
    置 已拒绝空应用名 为 真；
终

测试.断言（已拒绝空应用名，「应用名不得为空」）；

法 错误预算配置（） 则
    言枢.配置（）.设最大请求正文字节（1.5）；
    归 空；
终

测试.相等（取错误代码（错误预算配置），「YANSHU_LIMIT_CONFIG」）；

定 请求限制应用 为 言枢.创建应用（言枢.配置（）.设最大请求正文字节（2））；

请求限制应用.发（「/echo」，回声）；

测试.相等（言枢.测试客户端（请求限制应用）.发（「/echo」，「三个字节」，「text/plain; charset=utf-8」）.状态码，413）；

定 响应限制应用 为 言枢.创建应用（言枢.配置（）.设最大响应正文字节（4））；

响应限制应用.取（「/long」，长响应）；

测试.相等（言枢.测试客户端（响应限制应用）.取（「/long」）.状态码，500）；

法 路由超限（） 则
    定 所应用 为 言枢.创建应用（言枢.配置（）.设最大路由（1））；
    所应用.取（「/one」，首页）；
    所应用.取（「/two」，首页）；
    归 空；
终

测试.相等（取错误代码（路由超限），「YANSHU_LIMIT_ROUTE」）；

法 中间件超限（） 则
    定 所配置 为 言枢.配置（）.设自动安全首部（假）.设最大中间件（0）；
    定 所应用 为 言枢.创建应用（所配置）；
    所应用.使用（空中间件）；
    归 空；
终

测试.相等（取错误代码（中间件超限），「YANSHU_LIMIT_MIDDLEWARE」）；
