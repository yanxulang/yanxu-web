引「../src/言序Web.yx」为 言枢；

引「标准:测试」为 测试；

引「标准:字节」为 字节；

令 顺序：列<文> 为 【】；

令 已执行端点：理 为 假；

法 外层（上下文，下一步） 则
    追加（顺序，「外入」）；
    定 响应 为 下一步（）；
    追加（顺序，「外出」）；
    归 响应；
终

法 内层（上下文，下一步） 则
    追加（顺序，「内入」）；
    定 响应 为 下一步（）；
    追加（顺序，「内出」）；
    归 响应；
终

法 端点（上下文） 则
    置 已执行端点 为 真；
    追加（顺序，「端点」）；
    归 言枢.文字响应（「完成」）；
终

定 应用 为 言枢.应用（）；

应用.使用（外层）.使用（内层）.取（「/」，端点）；

定 响应 为 言枢.测试客户端（应用）.取（「/」）；

测试.相等（响应.状态码，200）；

测试.相等（字节.转文字（响应.正文），「完成」）；

测试.相等（长度（顺序），5）；

测试.相等（顺序【0】，「外入」）；

测试.相等（顺序【1】，「内入」）；

测试.相等（顺序【2】，「端点」）；

测试.相等（顺序【3】，「内出」）；

测试.相等（顺序【4】，「外出」）；

测试.断言（已执行端点，「端点应在中间件内部执行」）；

法 短路（上下文，下一步） 则
    归 言枢.状态响应（401，「拒绝」）；
终

置 已执行端点 为 假；

定 短路应用 为 言枢.应用（）；

短路应用.使用（短路）.取（「/」，端点）；

定 短路响应 为 言枢.测试客户端（短路应用）.取（「/」）；

测试.相等（短路响应.状态码，401）；

测试.相等（已执行端点，假）；

测试.相等（言枢.重定向（「/next」）.状态码，302）；

测试.相等（言枢.永久重定向（「/next」）.状态码，301）；

测试.相等（言枢.空响应（）.状态码，204）；
