引「../src/言序Web.yx」为 言枢；
引「标准:测试」为 测试；
引「标准:字节」为 字节；
引「标准:JSON」为 JSON；
引「标准:文件」为 文件；

令 模板根：文 为「tests/fixtures/templates」；
若 文件.存在（「yanxu-web/tests/fixtures/templates/detail.yb.html」）则
    置 模板根 为「yanxu-web/tests/fixtures/templates」；
终

定 配置项 为 言枢.配置（）.设应用名（「言枢规格」）.设模板根（模板根）；
定 所应用 为 言枢.创建应用（配置项）；

法 详情（上下文）则
    归 所应用.模板响应（「detail.yb.html」，{
        「id」：上下文.参数值（「id」），
        「self」：上下文.反向（「api:详情」，{「id」：上下文.参数值（「id」）}）
    }）；
终

法 新建（上下文）则
    定 输入：典 为 上下文.JSON正文（）；
    归 言枢.JSON响应（{「title」：输入【「title」】}）；
终

法 自定未找到（上下文）则
    归 所应用.模板状态响应（404，「detail.yb.html」，{「id」：「missing」，「self」：上下文.反向（「api:详情」，{「id」：「missing」}）}）；
终

定 接口 为 所应用.命名路由组（「/api」，「api」）；
接口.命名取（「/posts/:id」，「详情」，详情）；
接口.命名发（「/posts」，「新建」，新建）；
所应用.设状态处理器（404，自定未找到）；

测试.相等（所应用.反向（「api:详情」，{「id」：「a b」}），「/api/posts/a%20b」）；

定 客户端 为 言枢.测试客户端（所应用）；
定 详情响应 为 客户端.取（「/api/posts/hello」）；
测试.相等（详情响应.状态码，200）；
测试.相等（字节.转文字（详情响应.正文），「<h1>hello</h1><a href=\"/api/posts/hello\">自身</a>\n」）；
测试.相等（详情响应.首部值（「x-content-type-options」），「nosniff」）；

定 新建响应 为 客户端.发JSON（「/api/posts」，{「title」：「言标」}）；
测试.相等（JSON.解析（字节.转文字（新建响应.正文））【「title」】，「言标」）；

定 不见 为 客户端.取（「/none」）；
测试.相等（不见.状态码，404）；
测试.相等（字节.转文字（不见.正文），「<h1>missing</h1><a href=\"/api/posts/missing\">自身</a>\n」）；

定 不许 为 客户端.请求（「PUT」，「/api/posts/hello」）；
测试.相等（不许.状态码，405）；
测试.相等（不许.首部值（「allow」），「GET」）；

测试.相等（言枢.言标.渲染（「<p>{{ 值 }}</p>」，{「值」：「<安全>」}），「<p>&lt;安全&gt;</p>」）；

