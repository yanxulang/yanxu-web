引「../src/言序Web.yx」为 言枢；

引「标准:测试」为 测试；

引「标准:字节」为 字节；

法 回显路径（上下文） 则
    归 言枢.文字响应（上下文.参数值（「path」））；
终

法 空处理（上下文） 则
    归 言枢.空响应（）；
终

定 应用 为 言枢.应用（）；

应用.命名取（「/assets/*path」，「资源」，回显路径）；

测试.相等（应用.反向（「资源」，{「path」：「a b/c+d」}），「/assets/a%20b/c%2Bd」）；

定 响应 为 言枢.测试客户端（应用）.取（「/assets/css/site.css」）；

测试.相等（字节.转文字（响应.正文），「css/site.css」）；

定 分组 为 应用.命名路由组（「/api」，「api」）；

分组.命名取（「/items/:id」，「详情」，空处理）；

测试.相等（应用.反向（「api:详情」，{「id」：「甲 乙」}），「/api/items/%E7%94%B2%20%E4%B9%99」）；

令 已拒绝重复路由：理 为 假；

试 则
    应用.取（「/assets/*path」，空处理）；
救 所误 则
    置 已拒绝重复路由 为 真；
终

测试.断言（已拒绝重复路由，「同方法同模式不得重复」）；

令 已拒绝重复名称：理 为 假；

试 则
    应用.命名取（「/other」，「资源」，空处理）；
救 所误 则
    置 已拒绝重复名称 为 真；
终

测试.断言（已拒绝重复名称，「路由名称不得重复」）；

令 已拒绝缺少参数：理 为 假；

试 则
    应用.反向（「资源」，{}）；
救 所误 则
    置 已拒绝缺少参数 为 真；
终

测试.断言（已拒绝缺少参数，「反向路由必须提供动态参数」）；

令 已拒绝未知名称：理 为 假；

试 则
    应用.反向（「未知」，{}）；
救 所误 则
    置 已拒绝未知名称 为 真；
终

测试.断言（已拒绝未知名称，「反向路由必须拒绝未知名称」）；

令 已拒绝错误前缀：理 为 假；

试 则
    应用.路由组（「api」）；
救 所误 则
    置 已拒绝错误前缀 为 真；
终

测试.断言（已拒绝错误前缀，「路由组前缀必须以斜线开头」）；

令 已拒绝中途通配：理 为 假；

试 则
    应用.取（「/bad/*path/tail」，空处理）；
救 所误 则
    置 已拒绝中途通配 为 真；
终

测试.断言（已拒绝中途通配，「通配参数只能位于路由末尾」）；

令 已拒绝空参数名：理 为 假；

试 则
    应用.取（「/bad/:」，空处理）；
救 所误 则
    置 已拒绝空参数名 为 真；
终

测试.断言（已拒绝空参数名，「路径参数必须有名称」）；
