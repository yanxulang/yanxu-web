引「../src/言序Web.yx」为 言枢；

引「包:yanxu-http」为 HTTP；

引「标准:测试」为 测试；

引「标准:字节」为 字节；

法 成功（上下文） 则
    归 言枢.文字响应（「ok」）；
终

法 请求为（应用，方法：文，路径：文，首部：典） 则
    令 原文：文 为 （（（方法 加 「 」） 加 路径） 加 「 HTTP/1.1\nHost: testserver」）；
    逐 名称：文 于 键列（首部） 则
        置 原文 为 （（（原文 加 「\n」） 加 名称） 加 （「: 」 加 首部【名称】））；
    终
    归 应用.处理（HTTP.解析请求头（原文））；
终

定 令牌：文 为 「xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx」；

定 另一令牌：文 为 「yyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyy」；

定 配置项 为 言枢.CSRF配置（）.设可信来源（【「HTTPS://APP.EXAMPLE.COM:443/」】）；

测试.相等（配置项.可信来源【0】，「https://app.example.com」）；

定 令牌Cookie文：文 为 言枢.CSRF令牌Cookie（配置项，令牌）.渲染（）；

测试.相等（令牌Cookie文，（（「__Host-yanxu-csrf=」 加 令牌） 加 「; Path=/; Secure; SameSite=Strict」））；

测试.相等（言枢.删除CSRF令牌Cookie（配置项）.渲染（），「__Host-yanxu-csrf=; Path=/; Max-Age=0; Secure; SameSite=Strict」）；

测试.相等（长度（言枢.CSRF令牌（）），64）；

定 应用 为 言枢.创建应用（言枢.配置（））；

应用.使用（言枢.CSRF中间件（配置项））；

应用.取（「/change」，成功）；

应用.发（「/change」，成功）；

测试.相等（请求为（应用，「GET」，「/change」，{}）.状态码，200）；

测试.相等（请求为（应用，「GET」，「/change」，{「Sec-Fetch-Site」：「cross-site」}）.状态码，200）；

定 有效首部：典 为 {「Origin」：「https://app.example.com」，「X-CSRF-Token」：令牌，「Cookie」：（「__Host-yanxu-csrf=」 加 令牌）}；

定 有效响应 为 请求为（应用，「POST」，「/change」，有效首部）；

测试.相等（有效响应.状态码，200）；

测试.相等（字节.转文字（有效响应.正文），「ok」）；

测试.相等（请求为（应用，「POST」，「/change」，{「Referer」：「https://app.example.com/forms/edit?from=home」，「X-CSRF-Token」：令牌，「Cookie」：（「__Host-yanxu-csrf=」 加 令牌）}）.状态码，200）；

测试.相等（请求为（应用，「POST」，「/change」，{「Origin」：「https://evil.example」，「X-CSRF-Token」：令牌，「Cookie」：（「__Host-yanxu-csrf=」 加 令牌）}）.状态码，403）；

测试.相等（请求为（应用，「POST」，「/change」，{「X-CSRF-Token」：令牌，「Cookie」：（「__Host-yanxu-csrf=」 加 令牌）}）.状态码，403）；

测试.相等（请求为（应用，「POST」，「/change」，{「Origin」：「null」，「X-CSRF-Token」：令牌，「Cookie」：（「__Host-yanxu-csrf=」 加 令牌）}）.状态码，403）；

测试.相等（请求为（应用，「POST」，「/change」，{「Origin」：「https://app.example.com」，「Cookie」：（「__Host-yanxu-csrf=」 加 令牌）}）.状态码，403）；

测试.相等（请求为（应用，「POST」，「/change」，{「Origin」：「https://app.example.com」，「X-CSRF-Token」：另一令牌，「Cookie」：（「__Host-yanxu-csrf=」 加 令牌）}）.状态码，403）；

测试.相等（请求为（应用，「POST」，「/change」，{「Origin」：「https://app.example.com」，「X-CSRF-Token」：「short」，「Cookie」：「__Host-yanxu-csrf=short」}）.状态码，403）；

测试.相等（请求为（应用，「POST」，「/change」，{「Origin」：「https://app.example.com」，「Referer」：「https://app.example.com/forms/edit」，「Sec-Fetch-Site」：「cross-site」，「X-CSRF-Token」：令牌，「Cookie」：（「__Host-yanxu-csrf=」 加 令牌）}）.状态码，403）；

测试.相等（请求为（应用，「POST」，「/change」，{「Origin」：「https://evil.example」，「Referer」：「https://app.example.com/forms/edit」，「X-CSRF-Token」：令牌，「Cookie」：（「__Host-yanxu-csrf=」 加 令牌）}）.状态码，403）；

定 仅来源配置 为 言枢.CSRF配置（）.加可信来源（「https://app.example.com」）.设要求令牌（假）；

定 仅来源应用 为 言枢.创建应用（言枢.配置（））；

仅来源应用.使用（言枢.CSRF中间件（仅来源配置））；

仅来源应用.发（「/change」，成功）；

测试.相等（请求为（仅来源应用，「POST」，「/change」，{「Origin」：「https://app.example.com」}）.状态码，200）；

定 无来源配置 为 言枢.CSRF配置（）.设允许无来源（真）；

定 无来源应用 为 言枢.创建应用（言枢.配置（））；

无来源应用.使用（言枢.CSRF中间件（无来源配置））；

无来源应用.发（「/change」，成功）；

测试.相等（请求为（无来源应用，「POST」，「/change」，{「X-CSRF-Token」：令牌，「Cookie」：（「__Host-yanxu-csrf=」 加 令牌）}）.状态码，200）；

令 已拒绝空来源配置：理 为 假；

试 则
    言枢.CSRF中间件（言枢.CSRF配置（））；
救 所误 则
    置 已拒绝空来源配置 为 真；
终

测试.断言（已拒绝空来源配置，「默认 CSRF 配置必须声明可信来源」）；

令 已拒绝路径来源：理 为 假；

试 则
    言枢.CSRF配置（）.加可信来源（「https://app.example.com/path」）；
救 所误 则
    置 已拒绝路径来源 为 真；
终

测试.断言（已拒绝路径来源，「可信来源配置不得包含路径」）；

令 已拒绝不安全主机Cookie：理 为 假；

试 则
    言枢.CSRF中间件（言枢.CSRF配置（）.加可信来源（「https://app.example.com」）.设安全（假））；
救 所误 则
    置 已拒绝不安全主机Cookie 为 真；
终

测试.断言（已拒绝不安全主机Cookie，「__Host- CSRF Cookie 必须保持 Secure」）；

定 开发配置 为 言枢.CSRF配置（）.加可信来源（「http://localhost:8080」）.设令牌Cookie名（「yanxu-csrf」）.设安全（假）；

测试.相等（类型（言枢.CSRF中间件（开发配置）），「法」）；

定 开发应用 为 言枢.创建应用（言枢.配置（））；

开发应用.使用（言枢.CSRF中间件（开发配置））；

开发应用.发（「/change」，成功）；

测试.相等（请求为（开发应用，「POST」，「/change」，{「Origin」：「http://localhost:8080」，「X-CSRF-Token」：令牌，「Cookie」：（「yanxu-csrf=」 加 令牌）}）.状态码，200）；
