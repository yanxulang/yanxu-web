引「../src/言序Web.yx」为 言枢；

引「标准:测试」为 测试；

引「标准:JSON」为 JSON；

引「标准:字节」为 字节；

法 查看请求（上下文） 则
    归 言枢.JSON响应（{「方法」：上下文.请求.方法，「路径」：上下文.请求.路径，「查询」：上下文.查询值（「q」），「追踪」：上下文.首部值（「x-trace-id」），「主机」：上下文.首部值（「host」），「会话」：上下文.Cookie值（「sid」），「主题」：上下文.Cookie值（「theme」），「正文」：上下文.正文文字（）}）；
终

法 取错误代码（动作：法）：文 则
    试 则
        动作（）；
    救 所误 则
        归 言枢.错误详情（所误）【「代码」】；
    终
    归 「」；
终

定 应用 为 言枢.创建应用（言枢.配置（））；

应用.路由（「PATCH」，「/inspect」，查看请求）；

应用.取（「/cookies」，查看请求）；

定 客户端 为 言枢.测试客户端（应用）；

定 响应 为 客户端.请求字节为（「patch」，「/inspect?q=%E8%A8%80%E5%BA%8F」，{「X-Trace-ID」：「trace-42」，「Cookie」：「sid=session-1; theme=dark」，「Content-Type」：「text/plain; charset=utf-8」，「Host」：「app.example.test:8080」}，字节.从文字（「原始正文」））；

测试.相等（响应.状态码，200）；

定 数据：典 为 JSON.解析（字节.转文字（响应.正文））；

测试.相等（数据【「方法」】，「PATCH」）；

测试.相等（数据【「路径」】，「/inspect」）；

测试.相等（数据【「查询」】，「言序」）；

测试.相等（数据【「追踪」】，「trace-42」）；

测试.相等（数据【「主机」】，「app.example.test:8080」）；

测试.相等（数据【「会话」】，「session-1」）；

测试.相等（数据【「主题」】，「dark」）；

测试.相等（数据【「正文」】，「原始正文」）；

定 Cookie响应 为 客户端.请求Cookie（「GET」，「/cookies」，{「theme」：「light」，「sid」：「session-2」}）；

定 Cookie数据：典 为 JSON.解析（字节.转文字（Cookie响应.正文））；

测试.相等（Cookie数据【「会话」】，「session-2」）；

测试.相等（Cookie数据【「主题」】，「light」）；

法 伪造长度（）：空 则
    客户端.请求字节为（「POST」，「/inspect」，{「Content-Length」：「999」}，字节.从文字（「x」））；
    归 空；
终

测试.相等（取错误代码（伪造长度），「YANSHU_TEST_HEADER」）；

法 伪造分块（）：空 则
    客户端.请求为（「GET」，「/inspect」，{「Transfer-Encoding」：「chunked」}）；
    归 空；
终

测试.相等（取错误代码（伪造分块），「YANSHU_TEST_HEADER」）；

测试.相等（客户端.请求Cookie（「GET」，「/cookies」，{}）.状态码，200）；
