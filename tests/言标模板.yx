引「../src/言标.yx」为 言标；
引「标准:测试」为 测试；
引「标准:文件」为 文件；

定 源码：文 为「<h1>{{ 页面.标题 }}</h1>{# 不输出 #}{% 若 已发布 %}<p>{{ 摘要|默认:暂无 }}</p>{% 否则 %}<p>草稿</p>{% 终若 %}<ol>{% 逐 文章 于 文章列 %}<li data-index=\"{{ 循环.索引 }}\">{{ 文章.title }}{% 若 循环.首项 %}（首篇）{% 终若 %}</li>{% 终逐 %}</ol>」；
定 数据：典 为 {
    「页面」：{「标题」：「<言标>」}，
    「已发布」：真，
    「摘要」：「」，
    「文章列」：【
        {「title」：「甲 & 乙」}，
        {「title」：「丙」}
    】
}；

测试.相等（
    言标.渲染（源码，数据），
    「<h1>&lt;言标&gt;</h1><p>暂无</p><ol><li data-index=\"0\">甲 &amp; 乙（首篇）</li><li data-index=\"1\">丙</li></ol>」
）；

测试.相等（
    言标.渲染（「{{ 值 }}」，{「值」：言标.信任HTML（「<strong>可信</strong>」）}），
    「<strong>可信</strong>」
）；

令 模板根：文 为「tests/fixtures/templates」；
若 文件.存在（「yanxu-web/tests/fixtures/templates/list.yb.html」）则
    置 模板根 为「yanxu-web/tests/fixtures/templates」；
终
定 环境 为 言标.环境（模板根）；
测试.相等（
    环境.渲染文件（「list.yb.html」，{「文章列」：【{「title」：「<甲>」}，{「title」：「乙」}】}），
    「<ul><li><article>&lt;甲&gt;</article>\n</li><li><article>乙</article>\n</li></ul>\n」
）；

测试.相等（
    环境.渲染文件（「child.yb.html」，{「标题」：「<继承>」}），
    「<!doctype html><main><h1>&lt;继承&gt;</h1>\n</main>\n」
）；

令 已拒绝穿越：理 为 假；
试 则
    环境.渲染文件（「../list.yb.html」，{}）；
救 所误 则
    置 已拒绝穿越 为 真；
终
测试.断言（已拒绝穿越，「言标文件加载必须拒绝目录穿越」）；

令 已拒绝循环：理 为 假；
试 则
    环境.渲染文件（「cycle-a.yb.html」，{}）；
救 所误 则
    置 已拒绝循环 为 真；
终
测试.断言（已拒绝循环，「言标必须拒绝循环包含」）；

