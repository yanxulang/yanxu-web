引「../src/言序Web.yx」为 言枢；

引「../src/言访测试.yx」为 言访测试；

引「包:言据」为 言据；

引「标准:测试」为 测试；

引「标准:字节」为 字节；

法 查看请求（上下文） 则
    归 言枢.JSON响应（{「q」：上下文.查询值（「q」），「追踪」：上下文.首部值（「x-trace-id」），「会话」：上下文.Cookie值（「sid」）}）；
终

法 回显JSON（上下文） 则
    归 言枢.JSON响应（上下文.JSON正文（））；
终

法 回显言据（上下文） 则
    归 言枢.言据值响应（上下文.言据正文（））；
终

法 去终点（上下文） 则
    归 言枢.重定向（「/final」）；
终

法 终点（上下文） 则
    归 言枢.JSON响应（{「done」：真}）；
终

法 多Cookie（上下文） 则
    定 响应 为 言枢.JSON响应（{「ok」：真}）；
    响应.添Cookie（言枢.Cookie（「session」，「one」）.设路径（「/」）.设安全（真）.设仅HTTP（真））；
    响应.添Cookie（言枢.Cookie（「theme」，「dark」）.设路径（「/」））；
    归 响应；
终

法 HEAD响应（上下文） 则
    归 言枢.文字响应（「不会进入 HEAD 正文」）；
终

定 应用 为 言枢.创建应用（言枢.配置（））；

应用.取（「/inspect」，查看请求）；

应用.发（「/json」，回显JSON）；

应用.发（「/yanju」，回显言据）；

应用.取（「/redirect」，去终点）；

应用.取（「/final」，终点）；

应用.取（「/cookies」，多Cookie）；

应用.路由（「HEAD」，「/head」，HEAD响应）；

定 客户端 为 言访测试.客户端（应用）；

定 能力：典 为 客户端.传输能力（）；

测试.相等（能力【「真实网络」】，假）；

测试.相等（能力【「受控重定向」】，真）；

测试.相等（能力【「重复响应首部」】，真）；

定 查看响应 为 客户端.取（「/inspect」）.查询项（「q」，「言序」）.首部（{「X-Trace-ID」：「trace-1」}）.Cookie（{「sid」：「session-1」}）.发送（）；

测试.相等（查看响应.解析JSON（）【「q」】，「言序」）；

测试.相等（查看响应.解析JSON（）【「追踪」】，「trace-1」）；

测试.相等（查看响应.解析JSON（）【「会话」】，「session-1」）；

定 JSON响应 为 客户端.发文（「/json」）.JSON正文（{「name」：「子衿」，「count」：2}）.发送（）；

测试.相等（JSON响应.解析JSON（）【「name」】，「子衿」）；

定 言据响应 为 客户端.发文（「/yanju」）.言据正文（{「格式」：「言据」，「有效」：真}）.发送（）；

测试.相等（言据响应.首部值（「content-type」），（言据.版本媒体类型 加 「; charset=utf-8」））；

测试.相等（言据响应.解析言据（）【「格式」】，「言据」）；

定 重定向响应 为 客户端.取（「/redirect」）.发送（）；

测试.相等（重定向响应.状态，200）；

测试.相等（长度（重定向响应.重定向历史），1）；

测试.相等（重定向响应.解析JSON（）【「done」】，真）；

定 Cookie响应 为 客户端.取（「/cookies」）.发送（）；

测试.相等（长度（Cookie响应.Cookie列（）），2）；

测试.相等（长度（Cookie响应.各首部值（「set-cookie」）），2）；

测试.相等（Cookie响应.Cookie列（）【0】【「名称」】，「session」）；

定 取首部响应 为 客户端.取首部（「/head」）.发送（）；

测试.相等（字节.长度（取首部响应.正文），0）；

测试.断言（（取首部响应.首部值（「content-length」） 是 文），「HEAD 须保留实体长度首部」）；
