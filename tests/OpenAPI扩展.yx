引「../src/言序Web.yx」为 言枢；

引「标准:测试」为 测试；

引「标准:字节」为 字节；

引「标准:JSON」为 JSON；

法 成功（上下文） 则
    归 言枢.JSON响应（{「ok」：真}）；
终

法 用户详情（上下文） 则
    归 言枢.JSON响应（{「id」：上下文.参数值（「id」）}）；
终

法 取错误代码（动作：法）：文 则
    试 则
        动作（）；
    救 所误 则
        归 言枢.错误详情（所误）【「代码」】；
    终
    归 「」；
终

定 原参数：列 为 【{「名称」：「id」，「位置」：「path」，「必需」：真}】；

定 详情说明 为 言枢.接口说明（「users.get」）.设摘要（「读取用户」）.设说明（「按编号读取单个用户」）.设标签（【「用户」，「查询」，「用户」】）.设参数（原参数）.设响应（{「200」：{「说明」：「成功」}，「404」：{「说明」：「不存在」}}）.设安全（【{「bearerAuth」：【】}】）.设扩展（「x-owner」，「identity」）；

置 原参数【0】【「名称」】 为 「polluted」；

定 创建说明 为 言枢.接口说明（「users.create」）.设摘要（「创建用户」）.设标签（【「用户」】）.设请求体（{「必需」：真，「媒体类型」：「application/json」}）.设响应（{「201」：{「说明」：「已创建」}}）；

定 应用 为 言枢.创建应用（言枢.配置（）.设应用名（「用户服务」））；

应用.取（「/health」，成功）；

应用.命名接口路由（「GET」，「/users/:id」，「用户详情」，详情说明，用户详情）；

应用.接口发（「/users」，创建说明，成功）；

定 初始快照：典 为 应用.接口快照（）；

测试.相等（初始快照【「应用名」】，「用户服务」）；

测试.相等（长度（初始快照【「路由」】），2）；

测试.相等（初始快照【「路由」】【0】【「标识」】，「users.get」）；

测试.相等（初始快照【「路由」】【0】【「方法」】，「GET」）；

测试.相等（初始快照【「路由」】【0】【「模式」】，「/users/:id」）；

测试.相等（初始快照【「路由」】【0】【「路由名称」】，「用户详情」）；

测试.相等（长度（初始快照【「路由」】【0】【「标签」】），2）；

测试.相等（初始快照【「路由」】【0】【「标签」】【0】，「用户」）；

测试.相等（初始快照【「路由」】【0】【「标签」】【1】，「查询」）；

测试.相等（初始快照【「路由」】【0】【「参数」】【0】【「名称」】，「id」）；

测试.相等（应用.反向（「用户详情」，{「id」：「42」}），「/users/42」）；

详情说明.设摘要（「外部修改」）；

置 初始快照【「路由」】【0】【「摘要」】 为 「快照修改」；

测试.相等（应用.接口快照（）【「路由」】【0】【「摘要」】，「读取用户」）；

法 重复标识（）：空 则
    应用.接口取（「/duplicate-id」，言枢.接口说明（「users.get」），成功）；
    归 空；
终

测试.相等（取错误代码（重复标识），「YANSHU_OPENAPI_ID」）；

测试.相等（言枢.测试客户端（应用）.取（「/duplicate-id」）.状态码，404）；

法 重复路由（）：空 则
    应用.接口取（「/users/:id」，言枢.接口说明（「users.duplicate-route」），成功）；
    归 空；
终

测试.相等（取错误代码（重复路由），「WEB_ROUTE」）；

测试.相等（长度（应用.接口快照（）【「路由」】），2）；

令 提供器输入：典 为 {}；

法 生成文档（快照：典）：典 则
    置 提供器输入 为 快照；
    置 快照【「路由」】【0】【「摘要」】 为 「提供器修改」；
    归 {「openapi」：「3.1.0」，「info」：{「title」：快照【「应用名」】，「version」：「1.0.0」}，「paths」：{「/users/{id}」：{「get」：{「operationId」：快照【「路由」】【0】【「标识」】，「summary」：快照【「路由」】【0】【「摘要」】}}，「/users」：{「post」：{「operationId」：快照【「路由」】【1】【「标识」】}}}}；
终

定 提供器 为 言枢.OpenAPI提供器（生成文档）；

定 文档：典 为 应用.生成OpenAPI（提供器）；

测试.相等（文档【「openapi」】，「3.1.0」）；

测试.相等（文档【「paths」】【「/users/{id}」】【「get」】【「operationId」】，「users.get」）；

测试.相等（提供器输入【「路由」】【0】【「摘要」】，「提供器修改」）；

测试.相等（应用.接口快照（）【「路由」】【0】【「摘要」】，「读取用户」）；

置 文档【「openapi」】 为 「polluted」；

测试.相等（应用.生成OpenAPI（提供器）【「openapi」】，「3.1.0」）；

应用.挂OpenAPI（「/openapi.json」，提供器）；

定 文档响应 为 言枢.测试客户端（应用）.取（「/openapi.json」）；

测试.相等（文档响应.状态码，200）；

测试.相等（文档响应.首部值（「content-type」），「application/vnd.oai.openapi+json; version=3.1; charset=utf-8」）；

测试.相等（JSON.解析（字节.转文字（文档响应.正文））【「openapi」】，「3.1.0」）；

测试.相等（长度（应用.接口快照（）【「路由」】），2）；

法 坏生成器（快照） 则
    归 「不是文档」；
终

法 生成错误（）：空 则
    应用.生成OpenAPI（言枢.OpenAPI提供器（坏生成器））；
    归 空；
终

测试.相等（取错误代码（生成错误），「YANSHU_OPENAPI_PROVIDER」）；

法 错误扩展（）：空 则
    言枢.接口说明（「bad.extension」）.设扩展（「owner」，「invalid」）；
    归 空；
终

测试.相等（取错误代码（错误扩展），「YANSHU_OPENAPI_METADATA」）；

法 错误标识（）：空 则
    言枢.接口说明（「1bad」）；
    归 空；
终

测试.相等（取错误代码（错误标识），「YANSHU_OPENAPI_METADATA」）；
