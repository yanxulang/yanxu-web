引「../src/言标.yx」为 言标；

引「标准:测试」为 测试；

测试.相等（言标.渲染（「{{ 值 }}」，{「值」：「<script>」}），「&lt;script&gt;」）；

测试.相等（言标.渲染（「{{ 值 }}」，{「值」：言标.信任HTML（「<strong>可信</strong>」）}），「<strong>可信</strong>」）；

测试.相等（言标.渲染（「{{ 名称|大写 }}」，{「名称」：「yanxu」}），「YANXU」）；

测试.相等（言标.渲染（「{% 若 非 启用 %}关闭{% 否则 %}开启{% 终若 %}」，{「启用」：假}），「关闭」）；

法 断言失败（动作：法）：理 则
    试 则
        动作（）；
    救 所误 则
        归 真；
    终
    归 假；
终

法 未闭合插值（） 则
    言标.渲染（「{{ 值」，{「值」：「甲」}）；
    归 空；
终

法 未知过滤器（） 则
    言标.渲染（「{{ 值|未知 }}」，{「值」：「甲」}）；
    归 空；
终

法 错误循环类型（） 则
    言标.渲染（「{% 逐 项 于 值 %}{{ 项 }}{% 终逐 %}」，{「值」：「不是列」}）；
    归 空；
终

法 未知命令（） 则
    言标.渲染（「{% 未知 %}」，{}）；
    归 空；
终

测试.断言（断言失败（未闭合插值），「未闭合插值必须失败」）；

测试.断言（断言失败（未知过滤器），「未知过滤器必须失败」）；

测试.断言（断言失败（错误循环类型），「循环数据必须是列」）；

测试.断言（断言失败（未知命令），「未知模板命令必须失败」）；
