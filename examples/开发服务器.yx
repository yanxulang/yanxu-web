引「../src/言序Web.yx」为 Web；
引「包:yanxu-html」为 HTML；

法 首页（上下文）则
    归 Web.HTML响应（HTML.文档（HTML.元素（「html」，【】，【
        HTML.元素（「body」，【】，【HTML.元素（「h1」，【】，【HTML.文字（「言序 Web」）】）】）
    】）））；
终

定 所应用 为 Web.应用（）；
所应用.取（「/」，首页）；
Web.开发服务器（所应用，「127.0.0.1:8080」）.运行（）；
