引「../src/言序Web.yx」为 言枢；

法 首页（上下文） 则
    归 言枢.言标源码响应（「<!doctype html><html lang="zh-CN"><body><h1>{{ 标题 }}</h1><a href="{{ 地址 }}">命名路由</a></body></html>」，{「标题」：「言枢开发服务器」，「地址」：上下文.反向（「首页」，{}）}）；
终

定 配置项 为 言枢.配置（）.设应用名（「言枢示例」）；

定 所应用 为 言枢.创建应用（配置项）；

所应用.命名取（「/」，「首页」，首页）；

言枢.服务器（所应用，「127.0.0.1:8080」）.运行（）；
